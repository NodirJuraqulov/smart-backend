NODE_ENV=production
PORT=5000

DB_HOST=<production DB host>
DB_PORT=3306
DB_NAME=stoyanka_db
DB_USER=<production DB user>
DB_PASSWORD=<KUCHLI, TASODIFIY PAROL — bu yerga yozilmaydi, deploy paytida generatsiya qilinadi>
DB_POOL_MIN=2
DB_POOL_MAX=20

JWT_SECRET=<64+ belgili tasodifiy hex string — deploy paytida generatsiya qilinadi, masalan: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))">
ENCRYPTION_KEY=<64 belgili tasodifiy hex string — deploy paytida generatsiya qilinadi>
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_DAYS=30

CORS_ORIGIN=https://sizning-domeningiz.uz

UPLOADS_MAX_SIZE_MB=2000

PLATFORM_DEFAULT_TIMEZONE=Asia/Tashkent

PUBLIC_BASE_URL=http://195.158.9.168:84

PAYME_ENABLED=false
CLICK_ENABLED=false

LED_ENABLED=true
LED_HOST=192.168.1.157
LED_PORT=10000
LED_TIMEOUT_MS=3000
LED_CLOCK_INTERVAL_MS=60000
LED_PAYMENT_CONFIRM_DELAY_MS=3000
